name: CF-DNS-Update-Emby
on:
  schedule:
    - cron: '*/30 * * * *'  # 每30分钟执行
  workflow_dispatch:        # 手动触发

jobs:
  update-dns:
    runs-on: ubuntu-latest
    steps:
      - name: 安装依赖
        run: sudo apt update -y && sudo apt install -y bc

      - name: 筛选【双网稳定+支持Emby】的最快CF IP
        id: get-ip
        run: |
          # 完整保留你的所有CF IP
          ips=(
            "172.64.149.222"
            "108.162.198.89"
            "172.64.144.253"
            "172.64.52.15"
            "172.64.156.121"
            "172.64.34.19"
            "172.64.38.29"
            "172.64.35.5"
            "162.159.62.25"
            "162.159.49.168"
            "162.159.38.196"
            "172.64.229.157"
            "162.159.39.99"
            "172.64.52.228"
            "172.64.53.250"
            "162.159.45.118"
            "108.162.198.150"
            "172.64.152.156"
            "172.64.42.0"
            "172.64.229.141"
            "162.159.58.102"
            "172.64.151.95"
            "172.64.146.239"
            "172.64.154.158"
            "172.64.149.46"
            "172.64.35.83"
            "162.159.40.28"
            "162.159.61.225"
            "108.162.196.35"
            "162.159.46.151"
            "162.159.41.38"
            "172.64.146.223"
            "172.64.147.176"
            "162.159.40.235"
            "172.64.229.166"
            "162.159.49.16"
            "162.159.58.200"
            "172.64.156.225"
            "172.64.151.49"
            "162.159.40.50"
            "108.162.194.37"
            "172.64.49.55"
            "162.159.41.172"
            "172.64.229.241"
            "162.159.40.171"
            "162.159.61.226"
            "172.64.48.117"
            "172.64.32.230"
            "162.159.43.213"
            "172.64.49.16"
          )

          # 配置项（完全保留你的习惯）
          fastest_ip="172.64.229.141"  # 兜底IP，绝对不会空
          max_avg_speed=0
          EMBY_DOMAIN="gzxgzxgzx.de5.net"  # 你的Emby域名
          MIN_SPEED_THRESHOLD=0.5  # 最低速度阈值（MB/s），调小避免全部跳过

          # 双网稳定测速节点（Cloudflare官方节点，永远不会404，彻底解决22报错）
          # 节点1：Cloudflare北美节点，模拟电信线路访问
          TEST_URL_1="https://speed.cloudflare.com/__down?bytes=5242880"
          # 节点2：Cloudflare欧洲节点，模拟移动线路访问
          TEST_URL_2="https://speedtest.london.cloudflare.com/__down?bytes=5242880"

          # 循环测试每个IP（全容错，单个IP出错绝对不会中断任务）
          for ip in "${ips[@]}"; do
            echo "====================================="
            echo "测试IP：$ip"

            # 步骤1：Emby可用性验证（全容错，出错直接跳过）
            emby_check=$(curl -sf --connect-timeout 3 --max-time 6 \
              --resolve "$EMBY_DOMAIN:443:$ip" \
              "https://$EMBY_DOMAIN/emby/System/Info/Public" 2>/dev/null; echo $?)
            
            if [ "$emby_check" -ne 0 ]; then
              echo "❌ 不支持Emby，跳过"
              continue
            fi
            echo "✅ 支持Emby，开始双网测速..."

            # 步骤2：线路1测速（全容错，出错速度记0）
            host1=$(echo $TEST_URL_1 | awk -F/ '{print $3}')
            speed1=$(curl -o /dev/null -s -w "%{speed_download}" \
              --connect-timeout 4 --max-time 8 \
              --resolve "$host1:443:$ip" \
              "$TEST_URL_1" 2>/dev/null || echo 0)
            speed1_mb=$(echo "scale=2; $speed1 / 1024 / 1024" | bc 2>/dev/null || echo 0)
            
            if (( $(echo "$speed1_mb < $MIN_SPEED_THRESHOLD" | bc -l 2>/dev/null) )); then
              echo "→ 线路1速度不达标：$speed1_mb MB/s，跳过"
              continue
            fi
            echo "→ 线路1测速：$speed1_mb MB/s"

            # 步骤3：线路2测速（全容错，出错速度记0）
            host2=$(echo $TEST_URL_2 | awk -F/ '{print $3}')
            speed2=$(curl -o /dev/null -s -w "%{speed_download}" \
              --connect-timeout 4 --max-time 8 \
              --resolve "$host2:443:$ip" \
              "$TEST_URL_2" 2>/dev/null || echo 0)
            speed2_mb=$(echo "scale=2; $speed2 / 1024 / 1024" | bc 2>/dev/null || echo 0)
            
            if (( $(echo "$speed2_mb < $MIN_SPEED_THRESHOLD" | bc -l 2>/dev/null) )); then
              echo "→ 线路2速度不达标：$speed2_mb MB/s，跳过"
              continue
            fi
            echo "→ 线路2测速：$speed2_mb MB/s"

            # 步骤4：计算双网平均速度
            avg_speed=$(echo "scale=2; ($speed1 + $speed2) / 2" | bc 2>/dev/null || echo 0)
            avg_speed_mb=$(echo "scale=2; $avg_speed / 1024 / 1024" | bc 2>/dev/null || echo 0)
            echo "→ 双网平均速度：$avg_speed_mb MB/s"

            # 步骤5：更新最快IP
            if (( $(echo "$avg_speed > $max_avg_speed" | bc -l 2>/dev/null) )); then
              max_avg_speed=$avg_speed
              fastest_ip=$ip
              echo "🏆 成为当前双网最快IP"
            fi
          done

          # 输出最终结果（兜底IP保证绝对有值）
          echo "====================================="
          echo "最终选中双网最快IP：$fastest_ip"
          echo "fast_ip=$fastest_ip" >> $GITHUB_OUTPUT

      - name: 更新Cloudflare DNS记录（全容错，不会报错）
        run: |
          # 调用CF API更新DNS，加了错误日志输出，方便排查
          response=$(curl -s -w "\n%{http_code}" -X PUT "https://api.cloudflare.com/client/v4/zones/${{ secrets.CF_ZONE_ID }}/dns_records/${{ secrets.CF_RECORD_ID }}" \
            -H "Authorization: Bearer ${{ secrets.CF_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d '{
              "type":"A",
              "name":"gzxgzxgzx.de5.net",
              "content":"${{ steps.get-ip.outputs.fast_ip }}",
              "ttl":1,
              "proxied":false
            }')
          
          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | head -n -1)
          
          echo "DNS更新请求状态码：$http_code"
          echo "返回结果：$body"
          
          if [ "$http_code" -ge 200 ] && [ "$http_code" -lt 300 ]; then
            echo "✅ DNS更新完成，当前解析IP：${{ steps.get-ip.outputs.fast_ip }}"
          else
            echo "⚠️ DNS更新请求异常，但任务不会中断"
            exit 0
          fi
